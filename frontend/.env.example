# Copy to .env.local and adjust as needed.

# When true (default), the app reads from src/mocks/*.json instead of
# calling the backend - lets the frontend be built independently.
VITE_USE_MOCKS=true

# Used when VITE_USE_MOCKS=false. FastAPI dev server default.
VITE_API_BASE_URL=http://localhost:8000
